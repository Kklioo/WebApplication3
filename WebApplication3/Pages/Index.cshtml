@* @page
@model IndexModel
@inject IHttpContextAccessor contxt;

@{
    ViewData["Title"] = "Home page";
}

<div class="container mt-5">
    <div class="text-center">
        <h1 class="display-4">Welcome, @contxt.HttpContext.Session.GetString("FullName")</h1>

        <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>

        <div class="card">
            <div class="card-body">
                <h3 class="card-title">User Information:</h3>
                <ul class="list-group">
                    <li class="list-group-item"><strong>Email:</strong> @contxt.HttpContext.Session.GetString("Email")</li>
                    <li class="list-group-item"><strong>Gender:</strong> @contxt.HttpContext.Session.GetString("Gender")</li>
                    <li class="list-group-item"><strong>Phone Number:</strong> @contxt.HttpContext.Session.GetString("PhoneNumber")</li>
                    <li class="list-group-item"><strong>Delivery Address:</strong> @contxt.HttpContext.Session.GetString("DeliveryAddress")</li>
                    <li class="list-group-item"><strong>About Me:</strong> @contxt.HttpContext.Session.GetString("AboutMe")</li>
                    <li class="list-group-item" id="creditCardItem"><strong>Credit Card:</strong> @contxt.HttpContext.Session.GetString("CreditCardNo")</li>
                </ul>

                <!-- Add a link/button to trigger credit card decryption -->
                <a asp-page-handler="DecryptCreditCard" class="btn btn-primary mt-3">Decrypt Credit Card</a>

                <!-- Display the decrypted credit card information if available -->
                @if (ViewData["DecryptedCreditCard"] != null)
                {
                    <p class="mt-3"><strong>Decrypted Credit Card:</strong> @ViewData["DecryptedCreditCard"]</p>
                }
            </div>
        </div>
    </div>
</div> *@

@page
@model IndexModel
@inject IHttpContextAccessor contxt;

@{
    ViewData["Title"] = "Home page";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-body">
                    <h1 class="card-title text-center">Welcome, @contxt.HttpContext.Session.GetString("Email")</h1>

                    <p class="card-text text-center">Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>

                    <ul class="list-group">
                        <li class="list-group-item"><strong>Email:</strong> @contxt.HttpContext.Session.GetString("Email")</li>
                        <li class="list-group-item"><strong>Phone Number:</strong> @contxt.HttpContext.Session.GetString("PhoneNumber")</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
